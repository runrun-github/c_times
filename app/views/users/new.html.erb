<br>
<h1>都合の入力</h1>
<p style = "text-align:center">都合が良い日時にチェックを入れてください</p>
<br>
<%= form_with model: @user, url: event_users_path(@event) do |form| %>
  <div class="form-group">
    <%= form.label :user_name, "名前" %>
    <%= form.text_field :user_name, required: true ,class: "main_input_text" %>
  </div>
  <br>
    <%= form.hidden_field :event_id, value: @event.id %>
  <table class = "table time_table">
    <thead>
      <tr>
        <th>時間帯</th>
        <% ["日", "月", "火", "水", "木", "金", "土"].each do |day| %>
          <th><%= day.capitalize %></th>
        <% end %>
      </tr>
    </thead>
    <tbody>
      <% ["1", "2", "lunch", "3", "4", "5", "6", "night"].each do |slot| %>
        <tr>
          <td><%= @time_slots_in_japanese[slot] %></td>  <!-- 日本語表示 -->
          
          <% ["sunday", "monday", "tuesday", "wednesday", "thursday", "friday", "saturday"].each do |day| %>
            <td>
              <%= form.check_box "#{day}_#{slot}" , class: "availavility_check"%>
            </td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>

  <%= form.submit "登録する" , class:"btn btn-primary" %>
<% end %>

<br>